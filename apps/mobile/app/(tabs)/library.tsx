import { ComingSoon } from "@/components/ui/ComingSoon";

export default function LibraryScreen() {
  return (
    <ComingSoon
      title="My Library"
      icon="subscriptions"
      blurb="Connect a Stremio account and an IMDb list, then sync one into the other and watch the tally climb."
    />
  );
}
