import { ComingSoon } from "@/components/ui/ComingSoon";

export default function SettingsScreen() {
  return (
    <ComingSoon
      title="Settings"
      icon="settings"
      blurb="Profile, sync behaviour, OLED pure black, the motion preference, and your own TMDB and Trakt keys."
    />
  );
}
