import { ComingSoon } from "@/components/ui/ComingSoon";

export default function CalendarScreen() {
  return (
    <ComingSoon
      title="Release Calendar"
      icon="calendar_month"
      blurb="A month at a time: film releases and episode air dates, with every title for a given day one tap away."
    />
  );
}
